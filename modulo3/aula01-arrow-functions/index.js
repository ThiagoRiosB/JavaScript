const arrow = (a,b) => {
    return a + b
}
console.log (`Soma ${arrow(2,2)}`)

const subtract = (a, b) => a - b

console.log (`Subtração ${subtract(5,2)}`)

const dobro = n => `O dobro de ${n} é ${n*2}`
const number = 21

console.log(dobro(number))